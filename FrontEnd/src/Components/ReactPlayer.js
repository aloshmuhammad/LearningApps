import ReactPlayer from 'react-player';
const ReactPlayers = ({url}) => {
    return (
      <ReactPlayer
        url="url"
        controls={true}
        autoPlay={false}
      />
    );
  };
  export default ReactPlayers